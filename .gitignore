# secrets — 로컬만 (PUBLIC 레포 금지)
.env.local
.env
web/config.js

# assets (이미지스톡·폰트 = 골드레퍼런스에서 가져옴, 무겁다)
assets/

# deps / 빌드 산출물
node_modules/
out/

# 풀로 생성되는 전이 상태 (pull-supabase가 매번 갱신 — 커밋 불필요)
brand/_pulled-guidelines.txt
learnings/_inbox-feedback.json
learnings/00-approved.md

# 로컬 Claude Code 런타임 상태 (플러그인 정의 .claude-plugin/ 와 다름 — 그건 추적함)
.claude/

# OS
.DS_Store
Thumbs.db
